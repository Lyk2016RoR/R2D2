<% @books.each do |book| %>
<%=link_to book.name, book %>
    <%= book.name %><br/>
    <%= book.pages %><br/>
    <%= book.description %><br/>
    <%= book.comments.name %>

    <%= link_to "delete", book_path(book.id), method: :delete %><br/><br/><br/>
<% end %>
